# Pre-submission pre-analysis freeze

This record freezes the repository immediately before the post-primary H1 submission-finalization work requested on 2026-08-02. The authoritative baseline is commit `e69ce1c6ab0f9b93b43c6744391540830ed7ae0b`, annotated tag `pre-h1-finalization-20260802`, on the newly created branch `submission-prep-h1-finalization-20260802`.

The machine-readable companion records the tracked-file state, original and independent-seed scientific inputs, manuscript sources, SHA-256 checksums, and the execution environment. `input_checksums.sha256` provides a conventional checksum list.

The only pre-existing untracked item was `.claude/settings.local.json`. It is local tooling configuration and is deliberately excluded. Caches, virtual environments, downloaded dependencies, and unrelated temporary files are likewise excluded. No pre-existing scientific or manuscript change was present to snapshot.

The independent-seed exact data are preserved separately under `results/h2_replication_v1/_pipeline_output_stage1/raw/`; no result is pooled with the original production data. Existing primary, corrected, superseded, smoke-test, and robustness outputs remain in their established directories and are not overwritten.
