# Centered H3 interaction robustness plan

**Status:** post-primary explanatory/sensitivity audit, excluded from the corrected H1--H4 Holm family. The package already committed at `cbfb588` will be audited, not overwritten or reinterpreted by selecting favorable variants.

Inputs are `results/production_confirmatory/pointwise_gradient_statistics.parquet`, the frozen 443-draw `results/production_corrected_end_to_end/bootstrap_end_to_end_h2_h4_iterations.parquet`, the corrected coefficient table, and existing LOO outputs. Require unique `(analysis_mode,configuration_id,depth,budget,initialization_id,parameter_id)`, finite SNR eligibility, centered coding, all planned depths/budgets/configurations, and exactly one estimator mode per fit. End-to-end is adopted; conditional is diagnostic. Fits retain REML, optimizer order, initialization intercept, nested `(initialization,depth,parameter)` intercept, frozen `depth_z`, and corrected centered terms.

Derive from Patsy design rows: ER at L=0 (`E_c:R_c-.5 E_c:L_c:R_c`), L=1 (`E_c:R_c+.5 E_c:L_c:R_c`), and equal L-average (`E_c:R_c`). Full covariance supplies normal-reference Wald SE/CI/raw p; no within-package adjustment. The average must reproduce corrected H3 within `1e-10`. Transform frozen draws as `ER_L0=E:R`, `ER_L1=E:R+E:L:R`, and average `E:R+.5 E:L:R`; validate predeclared iterations 0--2 against existing explicit centered refits. No new bootstrap is authorized: unit, deterministic streams, 443 iterations, percentile interval, and failures are inherited and reported.

Active-residual sensitivity uses exactly D `{3,4,6}`, unchanged full-sweep scaling/eligibility/formula/random effects; no subset bootstrap. Depth heterogeneity uses one full-rank categorical centered ER-by-sum-coded-depth model, derives D=1/2/3/4/6 contrasts, equal-depth, eligible-observation, and predeclared active-depth summaries, plus model and initialization-clustered OLS joint Wald tests. Modes are never pooled. LOO uses 50 initialization deletions and reports extrema, sign/p crossings, maximum frozen-SE shift, and identifiers without exclusions.

Interpretation categories, in order of applicable evidence: robust across methods only if Wald/bootstrap/objective/mode/depth evidence is consistently nonzero/directional; objective-specific; active-depth-specific; model-dependent directional signal when Wald rejects but bootstrap or major sensitivities do not; unstable/inconclusive; or no evidence beyond primary Wald. Outputs remain under `results/h3_centered_robustness/` and `verification/h3_centered_interaction_robustness_results.{md,json}`, with checksums, plan/analysis commits, warnings, commands, and environment. Stop for contrast ambiguity, rank deficiency, mode pooling, changed eligibility/model, absent draw coefficients, checksum mismatch, or any need for a new bootstrap/model.
