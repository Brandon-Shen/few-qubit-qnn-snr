% Springer Nature journal article template (Version 3.1, December 2024)
% Compile with pdfLaTeX and BibTeX in a project containing sn-jnl.cls and
% sn-basic.bst from the official Springer Nature LaTeX template package.
\documentclass[pdflatex,sn-basic,Numbered]{sn-jnl}

\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{booktabs}
\usepackage{braket}
\usepackage{array}
\usepackage{tabularx}
\usepackage{enumitem}
\usepackage{seqsplit}
\usepackage[title]{appendix}

% PDF metadata; hyperref is loaded by the Springer Nature class.
\AtBeginDocument{\hypersetup{
  pdftitle={Interaction-Aware Benchmarking of Exact-Gradient Signal and Finite-Shot Resolvability in a Four-Qubit Hybrid Quantum Neural Network},
  pdfauthor={Brandon Shen},
  pdfsubject={Factorial benchmarking of exact-gradient signal and finite-shot resolvability in a four-qubit hybrid QNN},
  pdfkeywords={quantum machine learning, quantum neural networks, factorial design, finite-shot gradient estimation, gradient resolvability, reproducible benchmarking}
}}

\newcommand{\ttf}[1]{\texttt{\seqsplit{#1}}}
\newcommand{\cx}[2]{CX($#1\!\to\!#2$)}
\newcommand{\snrest}{\mathrm{SNR}_{\mathrm{est}}}
\newcommand{\snrexact}{\mathrm{SNR}_{\mathrm{exact}}}
\newcommand{\vshots}{\mathrm{Var}_{\mathrm{shots}}}

\setlist{nosep,leftmargin=*}
\setlength{\emergencystretch}{1.5em}
\raggedbottom

\begin{document}


\title[Interaction-Aware Benchmarking of QNN Gradients]
{Interaction-Aware Benchmarking of Exact-Gradient Signal and Finite-Shot Resolvability in a Four-Qubit Hybrid Quantum Neural Network}

\author*[1]{\fnm{Brandon} \sur{Shen} (ORCID: \href{https://orcid.org/0009-0002-3545-2106}{0009-0002-3545-2106})} \email{brandonshen123@gmail.com}

\affil*[1]{\orgname{Independent Researcher},
\orgaddress{\city{Belmont},
\state{California}, \postcode{94002}, \country{United States}}}

\abstract{\textbf{Purpose:} Quantum-neural-network gradient-mitigation strategies are commonly evaluated separately, although their joint effects may depend on circuit architecture, objective choice, and hybrid connectivity. We test for non-additive effects on exact-gradient signal and finite-shot gradient resolvability.

\textbf{Methods:} A complete $2^3$ factorial experiment crosses a pair-restricted within-block CNOT schedule, a normalized transverse-field Ising model energy objective, and a fixed residual shortcut in a four-qubit measure--re-encode--reset network. Configurations are matched by initialization, block count, parameter identity, and total shot budget. Outcomes include exact-gradient magnitude, estimator signal-to-noise ratio (SNR), bias, sign agreement, task metrics, and measurement cost.

\textbf{Results:} The pair-restricted schedule and energy objective show a positive interaction in exact-gradient magnitude ($\eta_{EL}=0.0043$, $p_{\mathrm{Holm}}=0.013$) in the original dataset, supported by the protocol-derived mixed-effects analysis and matched initialization bootstrap. For finite-shot SNR, the protocol-derived primary model estimates a positive interaction ($\beta_{EL}=0.025$, $p_{\mathrm{Holm}}=0.002$), but it is the only one of four distinct analyses to exclude zero. The nested bootstrap, initialization-level resampling, and cluster-robust reanalysis all include zero, and the estimand is conditional on excluding zero-variance cells that occur exclusively under the global-cost condition. An independent-seed rerun retains the positive pooled direction but not its magnitude or shallow-depth pattern. No interaction involving the residual shortcut is supported.

\textbf{Conclusion:} Factorial benchmarking can expose composition effects that isolated comparisons cannot identify. In this four-qubit case study, the exact-gradient interaction is supported by model-based and matched-resampling analyses of the original dataset. The finite-shot result is a model-dependent directional signal rather than an established interaction.}

\keywords{quantum machine learning, quantum neural networks, factorial design, finite-shot gradient estimation, gradient resolvability, reproducible benchmarking}

\maketitle

\section{Introduction}\label{sec:introduction}

Variational quantum algorithms and quantum neural networks (QNNs) optimize parameterized circuits through expectation values or their derivatives \cite{cerezo2021vqa,mitarai2018quantum,schuld2019evaluating}. Their practical behavior depends jointly on circuit structure, objective construction, initialization, optimization, and measurement. Barren plateaus are one important failure mode, but the absence of asymptotically vanishing gradients does not guarantee that a finite-shot optimizer receives a reliable update direction \cite{mcclean2018barren,anschuetz2022traps,thanasilp2023subtleties,larocca2025barren}. This study therefore asks a narrower operational question: whether a gradient component is resolvable under a specified finite-shot protocol.

Prior work has examined local objectives, restricted entanglement, structured initialization, hybrid architectures, and residual connections largely in separate models and against separate baselines \cite{cerezo2021costfunction,ortizmarrero2021entanglement,patti2021entanglement,grant2019identity,skolik2021layerwise,beer2020training,kashif2024resqnets}. Finite-shot studies likewise show that gradient precision and acquisition cost depend on the observable, circuit, and measurement allocation \cite{kubler2020adaptive,tamiya2022stochastic,kreplin2024finite,chinzei2025tradeoff,bowles2025backpropagation}. These results establish important marginal effects, but they do not determine whether interventions reinforce, cancel, or otherwise modify one another when combined.

A complete factorial design estimates such composition effects directly \cite{montgomery2019design}. We cross three interventions that act at different points in a hybrid computation: a pair-restricted within-block CNOT schedule ($E$), normalized TFIM energy in place of global target-state infidelity ($L$), and a fixed-gain classical residual shortcut between independently reset blocks ($R$). The experiment holds initialization seeds, matched quantum parameters, block counts, and total shot budgets fixed across all eight configurations.

The study follows the factors, hypotheses, model families, transformations, and multiplicity procedure described in a previously published protocol article \cite{shen2026gradient}. That article proposed the factorial experiment and reported no data or results from the implementation analyzed here. It did not constitute a complete preregistration: reset semantics, residual activation, plug-in-SNR eligibility, estimator-mode enforcement, and several robustness procedures were resolved during implementation or added after the primary outputs were inspected. We therefore describe H1--H4 as \emph{protocol-derived primary analyses conditional on the reset-per-block implementation}, not as unconditional preregistered tests, and label every later validation, sensitivity, and independent-seed analysis explicitly.

The contribution is threefold. First, the experiment directly estimates non-additive effects among three QNN gradient-resolvability strategies under matched conditions. Second, it separates exact-gradient magnitude, finite-shot resolvability, estimator bias, sign reliability, task metrics, and measurement cost rather than collapsing them into a single claim of ``trainability.'' Third, it provides an audited and reproducible implementation with deterministic seeds, black-box gradient validation, guarded estimator-mode separation, frozen outputs, and regression tests. The primary empirical result is a positive $E\times L$ exact-gradient interaction supported by both model-based and resampling analyses. The finite-shot analogue is presented as qualified evidence because it is conditional on estimator eligibility and sensitive to inference method and block count.

\section{Background and study questions}

\subsection{Finite-sample gradient usability}

For parameter $\theta_k$, let $\hat g_k(\theta)$ be the complete finite-shot gradient estimator and $\mu_k(\theta)=\mathbb{E}_{\mathrm{shots}}[\hat g_k(\theta)]$ its repeated-shot mean. The operational estimator-SNR metric is
\[
\snrest(\theta)=
\frac{|\mu_k(\theta)|}
{\sqrt{\vshots[\hat g_k(\theta)\mid\theta]}}.
\]
When an exact statevector derivative is available, the complementary measure is
\[
\snrexact(\theta)=
\frac{|g_k^{\mathrm{exact}}(\theta)|}
{\sqrt{\vshots[\hat g_k(\theta)\mid\theta]}},
\qquad
\mathrm{Bias}_k(\theta)=\mu_k(\theta)-g_k^{\mathrm{exact}}(\theta).
\]
High $\snrest$ indicates that the estimator's own mean is distinguishable from its repeated-shot uncertainty. It does not establish unbiasedness, correct sign, convergence, or task fidelity, so these outcomes are reported separately.

The plug-in ratio is defined only when estimated repeated-shot variance is positive. The finite-shot estimand is therefore conditional on that eligibility rule. Exactly zero-variance cells are excluded without an epsilon or variance floor, their occurrence is reported as a separate outcome, and a labeled sensitivity analysis assigns zero SNR when both the estimated mean and variance are exactly zero.

\subsection{Interventions and interaction hypotheses}

The pair-restricted CNOT factor ($E$) changes the within-block schedule while holding the number of rotations and logical CNOT gates fixed. The objective factor ($L$) replaces global infidelity with normalized TFIM energy; it is a full objective contrast that changes spectral weighting, observable decomposition, measurement requirements, and landscape geometry, not a pure locality intervention. The residual factor ($R$) is a fixed-gain classical shortcut between independently reset quantum blocks and adds no quantum gates or observables.

The novelty claim is intentionally narrow: the individual interventions and metrics are established. To our knowledge, these three implemented contrasts have not previously been evaluated together in a matched factorial experiment that separates exact-gradient magnitude from finite-shot estimator behavior. The strongest prior expectation concerned $E\times L$, because the entangling schedule changes reduced-state derivatives while the objective selects and weights the derivative components entering the measured signal. The published protocol expected sub-additivity at the exact-signal level, but all tests are two-sided \cite{shen2026gradient}.

H1--H4 form one Holm-corrected family at family-wise $\alpha=0.05$:
\begin{itemize}
\item \textbf{H1:} the $E\times L$ coefficient $\eta_{EL}$ in $\operatorname{asinh}(|g^{\mathrm{exact}}|)$ differs from zero;
\item \textbf{H2:} the $E\times L$ coefficient $\beta_{EL}$ in end-to-end $\operatorname{asinh}(\snrest)$ differs from zero;
\item \textbf{H3:} the $E\times R$ coefficient $\beta_{ER}$ differs from zero;
\item \textbf{H4:} the block-count-moderated $L\times R$ coefficient $\beta_{LRD}$ differs from zero.
\end{itemize}
Depth-moderation analyses beyond H4, conditional-estimator analyses, active-residual subset analyses, and configuration-level comparisons are diagnostic or exploratory.

\section{Evaluation protocol and experimental setup}\label{sec:methods}
\subsection{Reference task and objectives}

The reference task is ground-state preparation for the four-site, open-boundary TFIM,
\[
H=-J\sum_{i=0}^{2}Z_i Z_{i+1}-h\sum_{i=0}^{3}X_i,
\qquad J=1,\quad h=0.5.
\]
The target ground state $\ket{\psi_0}$ and eigenvalue bounds $E_0\approx-3.4270$ and $E_{\max}\approx3.4270$ are obtained by exact diagonalization of the $16\times16$ Hamiltonian matrix. Let $\rho_0=\ket{\psi_0}\!\bra{\psi_0}$ and let $\rho(\theta)$ be the prepared state. The global objective is target-state infidelity,
\[
C_{\mathrm{global}}(\theta)
=1-\mathrm{Tr}[\rho_0\rho(\theta)],
\]
and the local objective is normalized TFIM energy,
\[
C_{\mathrm{local}}(\theta)
=\frac{\mathrm{Tr}[H\rho(\theta)]-E_0}{E_{\max}-E_0}.
\]
Both objectives have the same nondegenerate target ground state, preventing target identity from confounding the comparison. The resulting factor still represents the full contrast between normalized TFIM energy and global infidelity, not measurement locality alone: the objectives also differ in spectral weighting, observable decomposition, measurement requirements, and landscape geometry. Final energy and global fidelity are therefore reported for every configuration regardless of which objective is optimized.

\subsection{Hybrid block architecture and entangling schedules}
\label{sec:ansatz}

To avoid conflict with the objective factor $L$, the number of hybrid blocks is denoted by $D\in\{1,2,3,4,6\}$. Each block applies one independently parameterized $R_y$ rotation to each qubit, followed by exactly four logical CNOT gates. $D$ is a \emph{hybrid block count}, not the depth of one continuously evolving quantum state: each block is reset, evaluated, and measured independently, and later blocks receive only classically re-encoded features. The results therefore apply specifically to the measure--re-encode--reset architecture defined below. Figure~\ref{fig:architecture} summarizes the dataflow and makes explicit that no quantum state crosses a block boundary.

\begin{figure*}[t]
\centering
\includegraphics[width=0.94\textwidth]{figures/fig_architecture_reset_blocks.pdf}
\caption{Measure--re-encode--reset architecture. Each quantum block begins from a fresh $\lvert 0000\rangle$ state, encodes a classical feature vector, applies parameterized rotations and one of the two four-CNOT schedules, and returns measured features $z^{(\ell)}$. Only classical features cross block boundaries. The residual path from $z^{(\ell-1)}$ first changes a realized block input at $D\geq3$}
\label{fig:architecture}
\end{figure*}

Two logical CNOT schedules are used within each block:

\begin{itemize}
\item \textbf{Baseline ($E=0$):} \cx{0}{1}, \cx{1}{2}, \cx{2}{3}, \cx{3}{0}, in that order. The sequential schedule contains a causal path spanning the register within the block.
\item \textbf{Restricted ($E=1$):} odd-position blocks use \cx{0}{1}, \cx{2}{3}, \cx{1}{0}, \cx{3}{2}; even-position blocks use \cx{1}{2}, \cx{3}{0}, \cx{2}{1}, \cx{0}{3}. Each block acts within two disjoint pairs, with the pairing alternating by block position.
\end{itemize}

Both schedules are applied literally, gate by gate, in the statevector simulator, with no transpiler-level fusion or reordering. Because no quantum state persists across blocks, alternating the restricted schedule changes the paired qubits at each block position but does not accumulate entanglement across $D$ in the manner of alternating layers in one continuous circuit. Entanglement entropy and reduced-state purity are used only as finite-system diagnostics; no area-law or asymptotic scaling claim is made from four qubits.

\subsection{Residual hybrid architecture}
\label{sec:residual-arch}

For block $\ell\in\{1,\ldots,D\}$, the quantum node is
\[
z_j^{(\ell)}
=\mathrm{Tr}\!\left[Z_j\rho_\ell
  \!\left(x^{(\ell)},\theta^{(\ell)}\right)\right],
\qquad j=0,\ldots,3.
\]
The block is initialized from $\ket{0\ldots0}$ and depends only on its own encoded input $x^{(\ell)}$ and gate angles $\theta^{(\ell)}$. For $\ell=1,\ldots,D-1$, the next input is
\[
x^{(\ell+1)}=
\begin{cases}
W_\ell z^{(\ell)}+b_\ell+\gamma z^{(\ell-1)}, & R=1,\\[3pt]
W_\ell z^{(\ell)}+b_\ell, & R=0,
\end{cases}
\]
with fixed shortcut gain $\gamma=1$ in the residual condition and $\gamma=0$ otherwise. $W_\ell$ and $b_\ell$ are parameterized and initialized identically across residual and non-residual conditions; the fixed shortcut adds no trainable parameters, quantum gates, or additional observables.

The boundary convention is $z^{(0)}=0$. Therefore, the shortcut first changes a real block input at $x^{(3)}=W_2z^{(2)}+b_2+\gamma z^{(1)}$, which exists only when $D\ge3$. Thus, under matched seeds, $R=0$ and $R=1$ are structurally identical at $D\in\{1,2\}$ and diverge only from $D=3$ onward.

This activation threshold was identified during the post-run implementation audit and was not specified in the published protocol article's H3/H4 analysis \cite{shen2026gradient}. It therefore does not retroactively redefine the protocol-derived target population. The original full-sweep models remain the primary analyses conditional on the reset-per-block implementation. A $D\ge3$ active-residual analysis is reported as a sensitivity analysis, and the exact zero residual contrast at $D\in\{1,2\}$ is reported as a structural-validity check.

\subsection{Gradient assembly and validation target}

The terminal block's gradient is a direct parameter-shift derivative of the final cost. Every upstream total derivative is assembled by reverse-mode chain rule through the classical maps and quantum-node Jacobians,
\[
\frac{dC}{d\theta_k^{(\ell)}}
=\sum_j
\frac{\partial C}{\partial z_j^{(\ell)}}
\frac{\partial z_j^{(\ell)}}{\partial\theta_k^{(\ell)}}.
\]
Node-level derivatives are evaluated by the two-term parameter-shift rule for Pauli-generated rotations \cite{mitarai2018quantum,schuld2019evaluating}; the downstream sensitivities are propagated exactly through the classical maps. Classical gradients with respect to $W_\ell$ and $b_\ell$ are analyzed separately and are not pooled into the matched quantum-gradient distribution.

The finite-difference audit treats the complete hybrid cost as a black box and compares central finite differences against the assembled total derivative. The audit design is described here; numerical outcomes are summarized in Section~\ref{sec:results-robustness}, with full details in the Supplementary Information.

\subsection{Complete factorial design}

All eight binary combinations of $E$, $L$, and $R$ are run (Table~\ref{tab:configurations}).

\begin{table*}[t]
\centering
\begin{tabular}{cccc l}
\toprule
\# & $E$ & $L$ & $R$ & Description \\
\midrule
1 & 0 & 0 & 0 & Baseline \\
2 & 1 & 0 & 0 & Pair-restricted CNOT schedule only \\
3 & 0 & 1 & 0 & TFIM-energy objective only \\
4 & 0 & 0 & 1 & Residual only \\
5 & 1 & 1 & 0 & $E+L$ (H1/H2 contrast) \\
6 & 1 & 0 & 1 & $E+R$ (H3 contrast) \\
7 & 0 & 1 & 1 & $L+R$ (H4 contrast) \\
8 & 1 & 1 & 1 & All three interventions \\
\bottomrule
\end{tabular}
\caption{Complete $2^3$ factorial design}
\label{tab:configurations}
\end{table*}

Within each hybrid block count, all configurations share initialization seeds, matched quantum-gate parameter indices, and the same nominal shot-budget levels.

\subsection{Finite-shot estimator modes}

Two finite-shot modes are collected and reported separately:

\begin{itemize}
\item \textbf{End-to-end mode, primary for H2--H4:} forward features and node-level shifted measurements are independently re-estimated in every replicate. Nonlinear downstream re-encoding can therefore induce finite-shot estimator bias.
\item \textbf{Conditional mode, diagnostic:} forward features are held at their exact statevector values while node-level shifted measurements are resampled. This isolates Jacobian-estimation noise from uncertainty caused by re-estimating and re-encoding intermediate features.
\end{itemize}

The conditional mode is not a second primary test and cannot replace an unfavorable end-to-end result. Material disagreement between the modes is interpreted as sensitivity to forward-feature sampling rather than resolved by selecting the more favorable estimator.

For every configuration, block count, initialization, matched quantum-gate parameter, and budget level $B\in\{250,500,1000,2000\}$, $R_{\mathrm{rep}}$ independent gradient replicates are generated. Their mean, sampling variance, $\snrest$, $\snrexact$, bias, and sign agreement are computed pointwise before aggregation.

\subsection{Measurement-budget accounting}
\label{sec:budget}

The symbol $B$ denotes the total shot budget for one replicate's complete gradient vector. The implementation floor-divides $B$ across every required measurement job and distributes the remainder deterministically one shot at a time, so the realized total shot count equals $B$ in every cell. The global and local objectives nevertheless require different numbers of observable bases, and end-to-end mode adds forward-feature measurement jobs. Consequently, configurations compared at the same $B$ can receive different shots per circuit even though their total-shot budgets are exactly matched. The implementation records shifted circuits, forward-feature circuits, observable settings, total jobs, and shots per circuit. Primary inference uses the protocol-derived $\log_2(B)$ factor; per-circuit precision and job-count differences are reported as resource diagnostics rather than treated as matched.

\subsection{Primary summaries and estimator-fidelity diagnostics}

The matched quantum-gradient distribution is summarized by the median and interquartile range of pointwise $\snrest$, the fraction of components with $\snrest\ge1$, and dependence on $D$ and $B$. The threshold one is descriptive only: it indicates that the estimator mean equals or exceeds one estimated sampling standard deviation and is not treated as a necessary or sufficient condition for optimization success.

Where exact derivatives are available, the analysis additionally reports $\snrexact$, signed and absolute bias, and
\[
\mathbb{1}\!\left[
\operatorname{sign}(\mu_k)
=\operatorname{sign}(g_k^{\mathrm{exact}})
\right].
\]
Exact-gradient landscape variance is calculated across matched initializations and remains distinct from repeated-shot variance at fixed parameters.

\subsection{Pilot-informed replicate and initialization counts}
\label{sec:calibration}

The independent pilot begins from $R_{\mathrm{rep}}=30$ replicates per cell and $N_{\mathrm{init}}=50$ matched initializations. Replicate count is increased until bootstrap interval widths for both the signed mean and $\snrest$ meet the prespecified precision criterion and stabilize across the next increment. Bootstrap resampling follows standard nonparametric principles \cite{davison1997bootstrap}; the criterion is motivated operationally by adaptive-shot work \cite{tamiya2022stochastic}. Because $\snrest$ is a plug-in ratio involving an estimated standard deviation, its own Monte Carlo uncertainty can remain large after the signed mean is already well resolved.

Candidate initialization counts are evaluated in increments of 25. The selected count is the smallest candidate for which the 90th percentile of the simulated 95\% confidence-interval half-width is at most 0.20 for every primary interaction coefficient. Actual convergence, failures to meet a criterion, and any recollection are Results items rather than assumed successes; the manuscript therefore uses \emph{pilot-informed}, not \emph{pilot-calibrated}, sample sizes.


\subsection{Analysis provenance and amendments}\label{sec:provenance}

The published protocol fixed the eight configurations, block-count and shot-budget sweeps, end-to-end estimator SNR as the primary finite-shot outcome, the mixed-effects model families, H1--H4, the $\operatorname{asinh}$ transformations, and Holm correction \cite{shen2026gradient}. It did not fix every operational detail. Most importantly, its prose permits both a reset-per-block reading, in which only classical features cross block boundaries, and a register-persisting reading, in which the quantum state evolves continuously across blocks (Supplementary Information, Appendix~A.2). We adopted the reset-per-block reading throughout this study (Section~\ref{sec:ansatz}). This substantive interpretive choice determines what the $E$ and $R$ factors act on; a register-persisting implementation would be a materially different experiment. The finite-difference audit establishes internal consistency only between our reset-per-block forward computation and gradient assembly. It does not establish that this reading matches the protocol authors' intent. Accordingly, H1--H4 are reported as protocol-derived primary analyses conditional on the reset-per-block implementation. Positive-variance eligibility, estimator-mode guards, active-residual subset fits, finite-difference checks, and the additional robustness procedures were resolved or added during the audit and are labeled separately. Full audit records are provided in the Supplementary Information and public repository.

\begin{table*}[t]
\centering
\small
\begin{tabularx}{\textwidth}{@{}p{0.24\textwidth}p{0.36\textwidth}X@{}}
\toprule
Analysis family & Provenance & Interpretive role \\
\midrule
H1--H4 mixed-model Wald/Holm tests & Factors, hypotheses, transformations, and model families were inherited from the published protocol; reset semantics were resolved during implementation & Protocol-derived primary decision rule, conditional on the reset-per-block implementation \\
Finite-difference checks, estimator-mode guards, optimizer refits, and residual diagnostics & Implementation audit & Validation and model diagnostics; not additions to the hypothesis family \\
$D\geq3$, $D\neq1$, and include-zero eligibility analyses & Added after primary-output inspection & Post-run sensitivity analyses \\
Cluster-robust inference, initialization-level resampling, independent-seed rerun, and depth moderation & Initiated after the primary analysis; each plan was frozen before its corresponding result was inspected & Post-run robustness, stability, or exploratory analyses \\
\bottomrule
\end{tabularx}
\caption{Analysis provenance and interpretive roles}
\label{tab:analysis-provenance}
\end{table*}

\subsection{Mixed-effects models}

Let $i$ index initialization, $c$ configuration, $p$ the matched quantum-gate parameter within a block count, $d$ block count, and $b$ shot budget. The three binary intervention indicators are effect coded as $\widetilde E_c,\widetilde L_c,\widetilde R_c\in\{-1/2,+1/2\}$, and $\widetilde D_d$ is block count centered and scaled to unit standard deviation. Effect coding makes lower-order coefficients average effects across the other factorial conditions. The matched repeated-measures structure is represented by a random intercept $u_i$ for initialization and a random intercept $v_{idp}$ for the matched parameter nested within initialization and block count. Mixed-effects estimation follows standard linear mixed-model practice \cite{bates2015lme4}. The 50 initializations are the independent top-level clusters; configuration, parameter, block-count, and budget rows are repeated observations rather than additional independent experimental units. The protocol-derived models contain random intercepts only and do not include random slopes.

For H1, the response is $a_{icpd}=\operatorname{asinh}(|g^{\mathrm{exact}}_{icpd}|)$. The model contains all intervention main effects, all pairwise products, the three-way product, centered block count, the three intervention-by-block-count terms, and the two random intercepts. The $E\times L$ coefficient is $\eta_{EL}$.

For H2--H4, the response among positive-variance-eligible cells is $y_{icpdb}=\operatorname{asinh}(\mathrm{SNR}_{\mathrm{est},icpdb})$, and $s_b=\log_2(B_b)$. The protocol-derived model is
\begin{align*}
y_{icpdb}={}&\beta_0
+\beta_E\widetilde E_c
+\beta_L\widetilde L_c
+\beta_R\widetilde R_c\\
&+\beta_{EL}\widetilde E_c\widetilde L_c
+\beta_{ER}\widetilde E_c\widetilde R_c
+\beta_{LR}\widetilde L_c\widetilde R_c\\
&+\beta_{ELR}\widetilde E_c\widetilde L_c\widetilde R_c
+\beta_D\widetilde D_d+\beta_B s_b\\
&+\beta_{ED}\widetilde E_c\widetilde D_d
+\beta_{LD}\widetilde L_c\widetilde D_d
+\beta_{RD}\widetilde R_c\widetilde D_d\\
&+\beta_{LRD}\widetilde L_c\widetilde R_c\widetilde D_d
+u_i+v_{idp}+\varepsilon_{icpdb}.
\end{align*}
H2 tests $\beta_{EL}=0$, H3 tests $\beta_{ER}=0$, and H4 tests $\beta_{LRD}=0$. The three-way coefficient is exploratory. The protocol-derived model uses the full $D$ sweep and is conditional on positive estimated replicate variance. A separate $D\ge3$ fit reports the same residual-related coefficients as a post-run sensitivity analysis.

All mixed-effects models are fit in Python 3.12.10 with statsmodels 0.14.6, REML, and an L-BFGS-first optimizer fallback order (BFGS, CG, Powell, Nelder--Mead attempted only if L-BFGS fails to converge); no custom convergence tolerances are set beyond each optimizer's library default. A fit is flagged singular if either variance component is within $10^{-8}$ of zero. Every primary and sensitivity fit in this paper converged and none was singular; an independent BFGS refit of the adopted H2--H4 model agrees with the default L-BFGS fit to 8--9 significant figures (Section~\ref{sec:results-robustness}). Full package versions, random-seed derivation, and bootstrap sharding are reported in the Software Availability and Reproducibility Information section below.

\subsection{Inference and multiplicity}

The protocol-derived primary decision rule uses model-based Wald standard errors and two-sided $p$-values. H1--H4 form one family, corrected by the sequential Holm procedure at family-wise $\alpha=0.05$ \cite{holm1979sequential}. Effect estimates, unadjusted confidence intervals, raw $p$-values, and Holm-adjusted $p$-values are all reported; a primary-model rejection requires the adjusted criterion. Failure to reject is not interpreted as equivalence without a prespecified smallest effect of interest and an equivalence test.

A nested matched bootstrap propagates finite-$R_{\mathrm{rep}}$ uncertainty into coefficient intervals \cite{davison1997bootstrap}. Outer resampling is by complete matched initialization, retaining all eight configurations, block counts, budgets, and parameter identities. Within each selected initialization--configuration--parameter--block-count--budget cell, gradient replicates are resampled; the pointwise mean, variance, and SNR are recomputed; and the full model is refit. Achieved iteration counts and Monte Carlo stability are reported rather than assumed.


\subsection{Post-run robustness and independent-seed rerun}\label{sec:h2-depth-methods}

The finite-shot $E\times L$ estimate was examined with four complementary procedures: a nested matched bootstrap that recomputes pointwise SNR after resampling, initialization-clustered robust regression, an independently implemented initialization-level resampling analysis, and a second dataset generated from a verified non-overlapping seed root. Additional models tested continuous and categorical moderation by block count, compared equal-depth and observation-count weighting, and repeated the analysis after assigning zero SNR to cells with exactly zero estimated mean and variance. These analyses were planned and frozen before their corresponding results were inspected, but they were initiated after the protocol-derived primary analysis and are therefore interpreted as robustness, stability, or sensitivity checks rather than additions to the primary hypothesis family.

\subsection{Implementation audit and use of large language models}

The implementation audit included black-box finite-difference validation of assembled hybrid gradients, inspection of reset semantics and residual activation, explicit estimator-mode guards, optimizer refits, deterministic seed checks, and regression testing of all principal analysis paths. Detailed audit history, superseded pooled-mode outputs, bootstrap checkpoints, and script-level provenance are reported in the Supplementary Information and repository rather than in the main narrative.

Anthropic's Claude was used during 2026 for literature-search planning, clarification of technical concepts, implementation and verification scripting, and feedback on author-written prose. It was not treated as an author or evidentiary source. The author independently inspected the cited literature, executed and reviewed the code, verified the numerical outputs, and accepts responsibility for every claim and analysis decision.

\section{Results}

\subsection{Calibration and estimator eligibility}\label{sec:results-calibration}\label{sec:zero-variance-sensitivity}

The initialization-count criterion was met at $N_{\mathrm{init}}=50$. The replicate-count criterion did not converge uniformly at the committed $R_{\mathrm{rep}}=30$, with the greatest instability at $D=1$. Raising the pilot ceiling to 500 replicates resolved only one of the four worst-affected shallow cells. The shallow-depth estimates should therefore be interpreted as less precisely calibrated than estimates at larger block counts.

The more consequential issue was factor-dependent eligibility. Exactly zero estimated replicate variance occurred only under the global-cost condition ($L=0$): 509 of 102,400 end-to-end cells and 1,324 of 102,400 conditional cells were excluded, whereas no $L=1$ cell was excluded in either mode (Table~\ref{tab:zero-variance-factor}). The finite-shot model is therefore a conditional analysis rather than an unconditional factorial average. Because eligibility is determined by whether estimated replicate variance is exactly zero, and that outcome is perfectly associated with the objective factor $L$, exclusion functions as selection on a factor level rather than an incidental data-quality filter: cells are not missing at random with respect to $L$. Consequently $\beta_{EL}$ should be read as a contrast restricted to the subpopulation of cells in which $L=0$ happens to produce resolvable variance, not as an estimate of the unconditional factorial interaction; this is a structural property of the plug-in SNR estimand, not an artifact that a larger sample would remove. Excluding $D=1$ leaves the signs of all three finite-shot target coefficients unchanged; $\beta_{EL}$ moves from $0.0250$ to $0.0324$, approximately one original standard error, while $\beta_{ER}$ and $\beta_{LRD}$ change negligibly.

\begin{table*}[t]
\centering
\small
\begin{tabularx}{0.82\textwidth}{@{}l>{\centering\arraybackslash}X>{\centering\arraybackslash}X@{}}
\toprule
Estimator mode & $L=0$: excluded/total (\%) & $L=1$: excluded/total (\%) \\
\midrule
End-to-end & $509/102{,}400$ ($0.497$) & $0/102{,}400$ ($0.000$) \\
Conditional & $1{,}324/102{,}400$ ($1.293$) & $0/102{,}400$ ($0.000$) \\
\bottomrule
\end{tabularx}
\caption{Exactly-zero replicate-variance exclusions by estimator mode and objective factor. The table describes eligibility for plug-in SNR analysis, not gradient quality or task performance}
\label{tab:zero-variance-factor}
\end{table*}

\subsection{Supported exact-gradient interaction}\label{sec:h1-result}

In the original dataset, the $E\times L$ interaction in exact-gradient magnitude is positive: $\eta_{EL}=0.004346$ (SE $0.001529$), $p=0.00448$, $p_{\mathrm{Holm}}=0.0134$. A matched bootstrap that resamples complete initializations, with 400 completed iterations, gives a percentile interval $[0.000810,0.008027]$, excluding zero in the same direction. Black-box finite differences agree with the assembled hybrid gradient to a worst relative error of $1.27\times10^{-5}$ overall and $9.77\times10^{-7}$ among non-floor-dominated components.

The interaction is super-additive on the transformed exact-gradient scale, contradicting the protocol article's directional expectation of sub-additivity. A post-run depth-moderation term is small and not distinguishable from zero, so the exact-gradient direction is not explained by pooling over block counts with opposing effects. On the secondary multiplicative scale, $J_{EL}=1.242$, corresponding to a 24.2\% super-additive shift in RMS exact-gradient magnitude. This is a descriptive point summary without a separate interval and does not establish a training-time or shot-budget saving. The exact-gradient interaction is the study's primary empirical finding in the original dataset.


\subsection{Qualified finite-shot interaction}\label{sec:h2-result}

Among positive-variance-eligible cells, the protocol-derived mixed model gives $\beta_{EL}=0.024996$ (SE $0.007279$), $p=0.000595$, $p_{\mathrm{Holm}}=0.00238$. The pooled direction matches the exact-gradient interaction, and separate conditional- and end-to-end-mode fits have similar positive point estimates among eligible cells.

This model-based rejection is not supported by the alternative inference procedures. The end-to-end nested bootstrap gives $[-0.018024,0.065688]$ after 443 completed iterations; the independently implemented initialization-level resampling interval is $[-0.003038,0.066606]$; and the initialization-clustered robust interval is $[-0.021739,0.068839]$. All include zero. Three of the four distinct analyses applied to this coefficient therefore fail to exclude zero, and only the protocol-derived Wald test does. We treat the coefficient as not robust across inferential methods, rather than describing the three alternative procedures as failed confirmations. Assigning the 509 excluded end-to-end cells zero SNR preserves the positive sign and protocol-derived Wald/Holm decision but shifts the estimate by about one original standard error.

Depth dependence is also unresolved. In the original dataset, mixed-model depth-specific contrasts are negative at $D=1,2$ and positive at $D=3,4,6$, but the omnibus depth-moderation result does not survive cluster-robust inference. An independent-seed rerun retains the positive pooled direction at approximately twice the original magnitude, yet is positive at all five block counts. The original and rerun datasets therefore agree at $D=3,4,6$ and disagree at the two shallowest depths, where replicate-count calibration was weakest.


The original pooled direction is also sensitive to how block counts are weighted. The equal-depth average is $-0.0101$ with a 95\% Wald interval $[-0.0222,0.0020]$, whereas the observation-count-weighted average is $0.0143$ with interval $[0.0042,0.0243]$; the adopted fully pooled mixed-model estimate is $0.0250$ with interval $[0.0107,0.0393]$. Deeper block counts contribute more matched parameters and therefore more rows, not more independent evidence. The independent-seed rerun is positive under both weighting schemes. These weighting differences reinforce that the original finite-shot pooled direction is not a stable, depth-neutral factorial summary.

Taken together, the finite-shot analyses support a positive pooled direction in the protocol-derived primary model but do not establish a stable, depth-independent interaction. Because eligibility is factor dependent and all three alternative intervals include zero, H2 should be interpreted as a model-dependent directional signal rather than as independent confirmation of the exact-gradient result.

\begin{table*}[t]
\centering
\footnotesize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.12}
\begin{tabularx}{\textwidth}{@{}llrrrr>{\raggedright\arraybackslash}Xcc@{}}
\toprule
Hyp. & Coef. & Est. & SE & $p$ & $p_{\mathrm{Holm}}$ & Bootstrap 95\% interval ($n$) & \shortstack{Primary Wald/Holm\\decision} & \shortstack{Bootstrap\\agreement} \\
\midrule
H1 & $\eta_{EL}$ & $0.00435$ & $0.00153$ & $0.0045$ & $0.0134$ & $[0.00081,0.00803]$ (400) & Reject & Yes \\
H2 & $\beta_{EL}$ & $0.02500$ & $0.00728$ & $0.0006$ & $0.0024$ & $[-0.01802,0.06569]$ (443) & Reject (model only) & No \\
H3 & $\beta_{ER}$ & $-0.00096$ & $0.00729$ & $0.896$ & $0.896$ & $[-0.03030,0.02448]$ (443) & Not rejected & N/A$^\dagger$ \\
H4 & $\beta_{LRD}$ & $-0.01018$ & $0.00536$ & $0.0577$ & $0.115$ & $[-0.02698,0.00640]$ (443) & Not rejected & N/A$^\dagger$ \\
\bottomrule
\end{tabularx}
\caption{Summary of the protocol-derived hypothesis family, conditional on the reset-per-block implementation. H2--H4 are conditional on positive estimated replicate variance. The primary column reports the protocol-derived model-based Wald/Holm rule; the bootstrap column reports agreement rather than a replacement decision. For H2, three of the four applied inference procedures (nested bootstrap, initialization-level resampling, cluster-robust) include zero; only the protocol-derived Wald test excludes it. We report the model-based decision because it is the prespecified primary rule, not because it is corroborated. $^\dagger$H3 and H4 have no rejection to corroborate; intervals containing zero are consistent with non-rejection but do not establish equivalence}
\label{tab:confirmatory-summary}
\end{table*}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig1_confirmatory_forest.pdf}
\caption{Model-based Wald intervals and matched-bootstrap percentile intervals for H1--H4. H1 is supported by both analyses. H2's protocol-derived Wald interval excludes zero, whereas its bootstrap interval includes zero; the discrepancy is reported directly rather than resolved by selecting the more favorable method}
\label{fig:confirmatory-forest}
\end{figure}

\subsection{Residual-shortcut and combined-strategy results}\label{sec:residual-results}

The protocol-derived primary analysis provides no evidence of an $E\times R$ interaction: $\beta_{ER}=-0.000958$, $p_{\mathrm{Holm}}=0.896$. Restricting the analysis to $D\ge3$, where the shortcut is structurally active, gives a similarly near-zero estimate ($-0.002128$, $p=0.761$). This is a non-rejection, not an equivalence result.

The block-count-moderated $L\times R$ coefficient is also not rejected: $\beta_{LRD}=-0.010179$, $p_{\mathrm{Holm}}=0.115$. Its near-boundary raw $p$-value is fragile: the magnitude falls by more than half in the $D\ge3$ subset, conditional and end-to-end fits have opposite signs, and the bootstrap interval includes zero. The data therefore do not support treating H4 as an effect awaiting only more resampling.

At the configuration level, the local TFIM-energy objective alone is the best single intervention in all 20 tested depth--budget cells. The fully combined configuration exceeds it in only 4 of 20 cells under the adopted end-to-end estimator. A positive pairwise interaction therefore does not imply that combining every available intervention produces the best system (Figure~\ref{fig:q1-comparison}).

\begin{figure*}[t]
\centering
\includegraphics[width=0.7\textwidth]{figures/fig5_q1_comparison.pdf}
\caption{Depth--budget grid showing where the fully combined configuration exceeds the best single-intervention configuration on RMS $\snrest$. The left panel shows the superseded pooled-mode result retained for audit transparency; the right panel shows the adopted end-to-end analysis, in which the win count narrows from 9/20 to 4/20}
\label{fig:q1-comparison}
\end{figure*}

\subsection{Estimator fidelity and measurement cost}

The restricted schedule produces lower entanglement entropy and higher reduced-state purity than the baseline schedule at every block count, validating the intended architectural contrast (Figure~\ref{fig:entanglement-diagnostic}). Neither series shows a distinguishable trend with block count, consistent with independently reset quantum blocks rather than a single state accumulating entanglement across layers.

\begin{figure*}[t]
\centering
\includegraphics[width=0.92\textwidth]{figures/fig3_entanglement.pdf}
\caption{Mean entanglement entropy and purity by block count for the baseline and restricted CNOT schedules, marginalized over the residual factor. Error bars are per-initialization standard errors ($n=50$)}
\label{fig:entanglement-diagnostic}
\end{figure*}

Conditional-mode estimates have higher sign agreement and lower absolute bias than end-to-end estimates in all eight configurations. Pooled sign agreement is 0.842, compared with 0.869 in conditional mode and 0.814 in end-to-end mode; median absolute bias is 0.00265 and 0.00416, respectively. Re-estimating and re-encoding intermediate features therefore changes the quality of the update signal supplied to an optimizer, not merely the precision of isolated node-level derivatives (Figure~\ref{fig:mode-split-bias}).

\begin{figure*}[t]
\centering
\includegraphics[width=0.92\textwidth]{figures/fig4_mode_split_bias.pdf}
\caption{Sign agreement and median absolute bias by configuration for conditional and end-to-end estimator modes. Conditional mode has higher sign agreement and lower absolute bias in all eight configurations}
\label{fig:mode-split-bias}
\end{figure*}

Total shots are matched exactly at each nominal budget, but shots per circuit are not. The energy objective requires two observable bases rather than one, and end-to-end estimation adds forward-feature jobs. These differences reduce per-circuit shots and widen with block count, reaching approximately a 27\% circuit-count difference between the two extremes at $D=6$. The resource claim is therefore limited to matched total shot count.
\subsection{Robustness and implementation checks}\label{sec:results-robustness}

Black-box finite differences validate the gradient assembly, and an independent BFGS refit reproduces the target mixed-model coefficients to 8--9 significant figures. The adopted fit is non-singular but shows heteroscedasticity by block count and shot budget and moderately heavy-tailed residuals, motivating the cluster-robust sensitivity analysis reported above. Leave-one-initialization-out fits preserve the sign of $\beta_{EL}$ in all 50 deletions, although the largest deletion shifts its magnitude by 1.13 original standard errors; this supports directional stability while reinforcing uncertainty in the precise finite-shot effect size.

A post-run audit also discovered that an early analysis had pooled conditional and end-to-end rows despite their distinct roles. Explicit mode guards were added, the affected analyses were rerun from frozen raw outputs, and regression tests reproduce both the bug and the corrected behavior. The family-level reject/non-reject pattern is unchanged, but H4's estimate and all finite-shot bootstrap results changed materially. This correction is retained as part of the scientific record rather than hidden in a silently revised output. Detailed diagnostics, checkpoint histories, superseded fits, and the complete audit log are provided in the Supplementary Information.

\section{Discussion}

\subsection{Principal findings}

The strongest result is the positive exact-gradient $E\times L$ interaction. It is supported in the original dataset by the protocol-derived mixed model, a matched bootstrap, black-box gradient validation, and a depth-moderation check that does not reveal an opposing trend. The finite-shot interaction has the same pooled direction and meets the protocol-derived Wald/Holm rule, but every alternative interval includes zero, the estimand is conditional on an eligibility process perfectly associated with objective type, and the depth-specific pattern is not retained at the two shallowest block counts in the independent-seed rerun. The appropriate conclusion is therefore asymmetric: the exact-gradient interaction is supported across primary and resampling analyses, whereas the finite-shot interaction is not established.

The effect sizes reinforce this distinction. The secondary exact-gradient index indicates a 24.2\% super-additive shift, whereas the finite-shot SNR index indicates a 3.4\% shift. The latter is operationally modest and sits atop uncertainty from estimator eligibility, residual heteroscedasticity, initialization clustering, and block-count moderation. Both indices are reported on the RMS gradient-magnitude scale rather than on a downstream training-cost scale, so their practical significance for optimizer step count, query budget, or convergence probability is not established by this study; a 24.2\% shift in initial gradient magnitude is a statistically supported descriptive point estimate for the original prepared-state sample, not a demonstrated training-time or shot-budget saving. Translating the interaction into those terms would require the optimization-trajectory experiments identified in Section~\ref{sec:limitations} as follow-up work. No robust interaction involving the fixed residual shortcut is identified, and the fully combined configuration does not consistently outperform the energy objective alone.

\subsection{Interpretation and relation to prior work}

Local objectives can improve gradient scaling under specified circuit-depth assumptions, while excessive random entanglement can suppress gradients \cite{cerezo2021costfunction,ortizmarrero2021entanglement,patti2021entanglement}. The present result connects these observations statistically: in this four-qubit reset-based architecture, the modeled objective contrast is larger under the restricted CNOT schedule than expected from the two marginal effects on the transformed response scale. This non-additivity is compatible with theoretical accounts that treat architecture, state structure, and observable support as coupled determinants of loss concentration \cite{ragone2024lie}, but it neither identifies a physical mechanism nor tests asymptotic barren-plateau scaling.

The $L$ factor also changes more than measurement locality. Normalized TFIM energy and global infidelity differ in spectral weighting, observable decomposition, circuit count, and landscape geometry. Likewise, the $E$ factor is one specific four-CNOT schedule and the $R$ factor is one fixed-gain shortcut between reset blocks. The observed coefficients should therefore be interpreted as interactions among these implemented contrasts, not universal laws about low entanglement, local costs, or residual quantum networks.

The results concern matched initial parameter points rather than complete optimization trajectories. A resolvable initial gradient is neither necessary nor sufficient for convergence, and residual architectures evaluated over full training trajectories address a different estimand \cite{anschuetz2022traps,grant2019identity,skolik2021layerwise,kashif2024resqnets}. The absence of a residual interaction here does not contradict optimization improvements reported in other architectures.

\subsection{Implications for QML evaluation}

The study shows why mitigation strategies should not be evaluated only one at a time. A factorial design can reveal pairwise non-additivity while also showing that the fully combined system is not optimal. It can additionally reveal moderation: one pooled finite-shot coefficient summarizes block counts whose estimated contrasts differ in sign in the original dataset. Future benchmarks should therefore test both whether interventions interact and whether those interactions vary with architecture-size variables.

Finite-shot metrics also require operational definitions that are treated as part of the experiment. Matching total shots does not match per-circuit precision; re-estimating intermediate features changes bias and sign reliability; and plug-in ratios can generate factor-dependent eligibility when estimated variance is zero. Eligibility rules, estimator modes, seed derivation, and regression guards should be specified and tested as carefully as the statistical formula itself.

\subsection{Limitations and priorities for follow-up}\label{sec:limitations}

All findings are limited to one four-qubit open-boundary TFIM task, a noiseless simulator, one reset-based hybrid architecture, and one implementation of each intervention. The design does not establish asymptotic scaling, hardware performance, or optimization convergence. Both the original and independent-seed rerun datasets contain only 50 independent initialization clusters, limiting cluster-robust and resampling precision regardless of the much larger number of pointwise rows.

A further limitation concerns protocol fidelity rather than generalizability: the published protocol's description of inter-block information flow is compatible with a register-persisting architecture that we did not implement (Section~\ref{sec:provenance}). Because this choice was made during implementation rather than fixed in advance, H1--H4 should be read as protocol-derived primary analyses conditional on the reset-per-block reading, not as an unconditional preregistered test of the protocol authors' design. We cannot rule out that a register-persisting implementation of the same nominal factors would produce different interaction estimates. The independent-seed rerun was used to assess finite-shot H2 stability; no second-seed H1 estimate is reported here.

The highest-priority follow-up is a prospectively specified $E\times L$ replication with substantially more independent initializations and replicate-count calibration tailored to shallow block counts. A continuous-state circuit control would test whether the exact-gradient interaction is specific to the measure--re-encode--reset architecture, and optimization-trajectory or hardware-noise experiments would establish whether initial gradient resolvability predicts behavior under more realistic training conditions.

\section{Conclusion}\label{sec:conclusion}

A complete factorial benchmark shows that the pair-restricted CNOT schedule and the TFIM-energy objective interact positively in exact-gradient magnitude within a four-qubit measure--re-encode--reset QNN.\@ Mixed-effects and matched-bootstrap analyses support this result, making it the study's primary empirical finding. The corresponding finite-shot coefficient meets the protocol-derived model decision rule but is the only one of four distinct analyses to exclude zero; combined with factor-dependent eligibility and inconsistent shallow-depth patterns, we do not treat it as an established interaction. No residual-shortcut interaction is supported, and combining all three strategies does not consistently outperform the best single intervention. More broadly, QNN benchmarking should estimate composition effects directly, separate exact signal from estimator fidelity and optimization performance, and treat measurement allocation and metric eligibility as part of the computational experiment.

\backmatter

\section*{Statements and Declarations}

\bmhead{Funding} No external funding was received.

\bmhead{Competing interests} The author declares no financial or non-financial competing interests.

\bmhead{Ethics approval} Not applicable. This study used computational simulations and did not involve human participants, human data, animals, or biological materials.

\bmhead{Consent to participate} Not applicable.

\bmhead{Consent for publication} Not applicable.

\bmhead{Author contributions} Brandon Shen conceived the study, implemented the simulation and analysis pipeline, conducted the experiments and verification analyses, interpreted the results, prepared the figures, and wrote and revised the manuscript.

% Before submission, mint a versioned archival DOI (for example via Zenodo) and add it here if available.
\bmhead{Data availability} The public repository contains the frozen production data, pointwise gradient and SNR statistics, primary-model outputs, sensitivity analyses, independent-seed rerun, checksums, and machine-readable diagnostics used for every reported number and figure. The finalized manuscript commit is recorded in \ttf{MANUSCRIPT\_COMMIT.txt}. The repository is available at \url{https://github.com/Brandon-Shen/few-qubit-qnn-snr}.

\bmhead{Code availability} The public repository contains the quantum simulation, gradient assembly, mixed-model and multiplicity code, bootstrap implementations, robustness and depth-moderation analyses, figure scripts, and regression-test suite: \url{https://github.com/Brandon-Shen/few-qubit-qnn-snr}.

\bmhead{Materials availability} Not applicable.

\bmhead{Software and reproducibility information} Analyses used Python 3.12.10, PennyLane 0.45.1, pandas 3.0.3, NumPy 2.5.1, statsmodels 0.14.6, SciPy 1.18.0, Matplotlib 3.11.0, PyArrow 25.0.0, and PyYAML 6.0.3. Random seeds are derived deterministically from a root seed, named stream, and cell identifiers via SHA-256. Every primary and sensitivity mixed model converged and none was singular; optimizer agreement, warnings, frozen inputs, and script-level provenance are documented in the Supplementary Information and repository.

\bmhead{Supplementary information} Online Resource 1 contains additional estimator-eligibility analyses, model and influence diagnostics, the documented estimator-mode correction, bootstrap and depth-sensitivity results, and a reproducibility index. The supplementary file is provided as \texttt{ESM\_1.pdf}.

\bibliography{references}

\end{document}